package com.tonikamitv.loginregister;

public class SimpleFormFragment {

    // Button click handler for the simple form
    /*public void reserveATime(View button) {
        // Do click handling here

        // Reading input from the EditText Controls
        final EditText startTimeField = (EditText) findViewById(R.id.EditTextStartTime);
        String startTime = startTimeField.getText().toString();

        final EditText endTimeField = (EditText) findViewById(R.id.EditTextEndtime);
        String endTime = endTimeField.getText().toString();

        final EditText descriptionField = (EditText) findViewById(R.id.EditTextDescription);
        String description = descriptionField.getText().toString();

        // Reading input from the Spinner Controls
        final Spinner kitchenItemSpinner = (Spinner) findViewById(R.id.SpinnerKitchenItem);
        String kitchenItem = kitchenItemSpinner.getSelectedItem().toString();

        // Reading input from CheckBox Controls
        final CheckBox servingCheckbox = (CheckBox) findViewById(R.id.CheckBoxServing);
        boolean bRequiresResponse = servingCheckbox.isChecked();
    }*/
}